# DriveMind Environment Variables Template - ALPHA-CODENAME Standards
# Copy this file to .env.local and fill in your values

# Google OAuth Configuration (Required)
GOOGLE_OAUTH_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=your-client-secret

# Firebase Configuration (auto-provided by Firebase Hosting in production)
NEXT_PUBLIC_FIREBASE_CONFIG={"projectId":"your-project-id","apiKey":"your-api-key","authDomain":"your-project.firebaseapp.com","storageBucket":"your-project.appspot.com","messagingSenderId":"123456789","appId":"your-app-id"}

# Environment Settings
NODE_ENV=development
LOG_LEVEL=debug

# Optional: Custom Base URL for OAuth redirects
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Optional: Gemini AI API Key (for AI features)
GEMINI_API_KEY=your-gemini-api-key
GOOGLE_AI_API_KEY=your-google-ai-api-key

# Admin API Security Token (Choose a strong random token)
ADMIN_API_TOKEN=choose-a-strong-random-token

# Genkit Configuration
GENKIT_ENV=dev

# Production Deployment Settings (Firebase App Hosting)
# These are automatically provided by Firebase in production:
# - PORT (app hosting port)
# - FIREBASE_CONFIG (Firebase configuration)
# - GCLOUD_PROJECT (Google Cloud project ID)

# SECURITY NOTES - ALPHA-CODENAME Requirements:
# - Never commit .env files to version control
# - Use Firebase secrets for production credentials
# - Rotate secrets regularly (quarterly minimum)
# - Use least-privilege access for API keys
# - Validate all environment variables at startup
# - Implement secret rotation support
