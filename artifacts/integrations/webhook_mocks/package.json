{
  "name": "drivemind-webhook-mocks",
  "version": "1.0.0",
  "description": "Mock webhook servers for DriveMind integration testing",
  "main": "google-drive-webhook-server.js",
  "scripts": {
    "start": "node google-drive-webhook-server.js",
    "dev": "nodemon google-drive-webhook-server.js",
    "gemini": "node gemini-mock-server.js",
    "firebase": "node firebase-mock-server.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "health": "curl -f http://localhost:8090/health || exit 1",
    "clean": "rm -rf logs/*.log"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  },
  "keywords": [
    "webhook",
    "mock",
    "google-drive",
    "firebase",
    "gemini",
    "testing"
  ],
  "author": "DriveMind Team",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}