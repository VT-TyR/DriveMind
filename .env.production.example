# Production Environment Variables for DriveMind
# This file documents the production environment setup

# OAuth Configuration (set via Firebase secrets in production)
# GOOGLE_OAUTH_CLIENT_ID=your-client-id.apps.googleusercontent.com
# GOOGLE_OAUTH_CLIENT_SECRET=your-client-secret

# Firebase Configuration (auto-injected by App Hosting)
# FIREBASE_CONFIG=auto-provided

# Production URLs
# NEXT_PUBLIC_BASE_URL=https://studio--drivemind-q69b7.us-central1.hosted.app

# Last updated: 2025-09-04
# OAuth credentials configured in Firebase secrets to match Google Cloud Console