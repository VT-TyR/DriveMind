specVersion: "v1"
serviceId: "drivemind"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "drivemind-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
